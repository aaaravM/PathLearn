[build]
provider = "nixpacks"
providers = ["python"]
paths = ["backend"]
# The manual buildCommand is REMOVED to let Nixpacks handle pip internally.

[deploy]
startCommand = "cd backend && python3 app.py"
